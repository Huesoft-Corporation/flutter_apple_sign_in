name: apple_sign_in_example
description: Demonstrates how to use the apple_sign_in plugin.
publish_to: 'none'

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  apple_sign_in:
    path: ../
flutter:
  uses-material-design: true
